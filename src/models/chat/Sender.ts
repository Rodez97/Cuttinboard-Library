export type Sender = {
  /**
   * UID del usuario
   */
  id: string;
  /**
   * Nombre del usuario
   */
  name: string;
  /**
   * Enlace a la imagen de perfil del usuario
   */
  avatar: string;
};
